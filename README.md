# Assignment-4
Task 1:
The code in Task1ANo4.py demonstrates basic file handling in Python using the with open() statement and exception handling.
- Opens a text file (sample.txt) in read mode.
- Reads the entire content of the file and prints it to the console.
- Handles the case where the file does not exist by catching the FileNotFoundError and displaying a user-friendly message.

Task 4:
The code in Task2ANo4.py demonstrates how to create, write, append, and read files in Python using the with open() statement.

- Write Mode ("w"): Creates a new file output.txt (or overwrites if it already exists) and writes user input into it.
- Append Mode ("a"): Adds additional user input to the same file without erasing existing content.
- Read Mode ("r"): Reads the final content of the file and displays it on the console.


